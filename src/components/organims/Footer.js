import React from "react";
import { css } from "emotion";
import { rem } from "polished";

const Footer = () => (
  <footer
    className={css({
      padding: rem("20px"),
    })}
  >
    Footer
  </footer>
);

export default Footer;
